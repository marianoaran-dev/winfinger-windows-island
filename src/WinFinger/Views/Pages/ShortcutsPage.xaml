<UserControl x:Class="WinFinger.Views.Pages.ShortcutsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:WinFinger.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2,0,2,10">
            <TextBlock Style="{StaticResource Text.Title}" Text="Shortcuts · " VerticalAlignment="Center"/>
            <TextBlock x:Name="AppNameLabel" Style="{StaticResource Text.Title}"
                       Foreground="{StaticResource Brush.Accent}" VerticalAlignment="Center" Text="Windows"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Style="{StaticResource Scroll.Dark}">
            <ItemsControl x:Name="GroupList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ShortcutGroup}">
                        <StackPanel Margin="2,0,10,14">
                            <TextBlock Style="{StaticResource Text.Micro}" Foreground="{StaticResource Brush.TextTertiary}"
                                       FontWeight="SemiBold" Text="{Binding Title}" Margin="2,0,0,6"/>
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:ShortcutItem}">
                                        <Border Background="{StaticResource Brush.Surface}" CornerRadius="10"
                                                Padding="10,7" Margin="0,0,6,6" MinWidth="150">
                                            <StackPanel Orientation="Horizontal">
                                                <Border Background="#22FFFFFF" CornerRadius="6" Padding="7,3">
                                                    <TextBlock FontFamily="{StaticResource Font.Mono}" FontSize="11"
                                                               Foreground="{StaticResource Brush.TextPrimary}"
                                                               Text="{Binding Keys}"/>
                                                </Border>
                                                <TextBlock Style="{StaticResource Text.Secondary}" Text="{Binding Action}"
                                                           VerticalAlignment="Center" Margin="9,0,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
